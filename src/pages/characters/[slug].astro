---
import Layout from "shared/ui/layout.astro";
import "styles/global.css";

export function getStaticPaths() {
  return [
    {params: {slug: '1'}},
    {params: {slug: '2'}},
    {params: {slug: '3'}},
  ];
}

const { slug } = Astro.params;
---


<Layout>
  <main>
    <h1 class="text-white">HELLO {slug}</h1>
  </main>
</Layout>